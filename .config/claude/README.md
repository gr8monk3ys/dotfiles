# Claude Code

Nothing is tracked here: Claude Code keeps its own state in `~/.claude/`.
The CLI is installed by its own installer into `~/.local/bin/claude`
(`.zshrc` puts that on `PATH`), and `aliases.zsh` defines `cc='claude'`.
Per-repo instructions go in that repo's `CLAUDE.md`; this repo's is at the root.
