#!/usr/bin/env bash
#
# is-executable - Compatibility shim for checking if a command exists.
#
# Usage:
#   is-executable <command>
#

set -euo pipefail

if [[ $# -lt 1 ]]; then
    echo "Usage: is-executable <command>" >&2
    exit 2
fi

command -v "$1" >/dev/null 2>&1
